# /build-module

Delegates implementation of the current active module to the Metaswarm multi-agent swarm.
Requires `/specify-module` to have already been run and the plan reviewed.

## Usage

```
/build-module
```

No arguments — reads the active feature from `.specify/feature.json`.

## Prerequisites

- `specs/<feature>/plan.md` exists and has been reviewed.
- `specs/<feature>/tasks.md` exists (generated by `/specify-module`).
- `specs/<feature>/research.md`, `data-model.md`, `contracts/` are all present.
- CLAUDE.md SPECKIT block points to the current feature.

## Steps

1. **Branch setup** (ALWAYS before any code):
   ```bash
   git checkout master
   git pull origin master
   git checkout -b feat/<feature-slug>   # e.g. feat/auth, feat/contacts
   ```
   - If working tree is dirty, stop and ask the user to resolve it first.
   - If the branch already exists (resuming interrupted work), just check it out — do NOT reset it.
   - Never implement on master.

2. **Verify prerequisites**: Read `.specify/feature.json` to confirm active feature directory.
   - Confirm plan.md and tasks.md exist. If missing, tell user to run `/specify-module` first.

2. **Load context for agents**:
   - Read `SYSTEM_STATE.md` — what prior modules built (schema, patterns, gotchas).
   - Read `specs/<feature>/plan.md` — technical decisions, file structure.
   - Read `specs/<feature>/tasks.md` — task phases and dependencies.
   - Read `specs/<feature>/contracts/` — Server Action contracts.
   - Read `specs/<feature>/data-model.md` — DB schema for this module.

3. **Invoke Metaswarm orchestration** via `/metaswarm:orchestrated-execution`:
   - Pass the full plan + tasks context to the swarm coordinator.
   - The swarm spawns parallel agents per task phase:
     - `architect-agent` — reviews plan, confirms structure
     - `coder-agent` — implements DB migrations, Server Actions, pages, components
     - `test-automator-agent` — writes unit tests + E2E specs
     - `code-review-agent` — reviews output before handoff
   - Agents use `stitch-design` skill for all UI (fetch Stitch screen before writing components).
   - Agents use `find-docs` skill for library API lookups (no training-data guessing).
   - Constitution gates enforced by architect-agent before any code is written.

4. **After swarm completes — run quality gates**:
   ```bash
   pnpm tsc --noEmit
   pnpm vitest run --coverage
   ```
   Fix any failures before proceeding.

5. **Run `/speckit-handoff`**:
   - Updates `SYSTEM_STATE.md` with new schema, actions, patterns, gotchas.
   - Writes `.beads/knowledge/` entries.

7. **Report and stop**:
   - Files changed, test results, SYSTEM_STATE.md sections updated.
   - Proposed git commands for user to run:
     ```bash
     git push -u origin feat/<feature-slug>
     # then open GitHub → Compare & pull request
     ```
   - After push confirmed: `bd close <id1> <id2> ...` for all completed issues.

## Rules

- Never commit or push automatically — always stop for human approval.
- Never skip quality gates or `/speckit-handoff`.
- One module per invocation — do not chain to next module automatically.
- `bd close` only after branch is pushed — never on local commit alone.
- Always branch from a clean, pulled master (Step 1). No exceptions.
