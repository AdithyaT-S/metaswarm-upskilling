{"id":"p001","module":"002-auth","title":"Server Action input validation order","body":"Always safeParse FIRST before any auth check or DB call. Order: safeParse → getAuthUser() → role check → business logic. Prevents leaking auth status to unauthenticated callers.","tags":["server-actions","auth","validation"]}
{"id":"p002","module":"002-auth","title":"Compensating saga for email+DB","body":"Insert invitation row → try send email → on failure DELETE the row. True atomicity impossible across DB+HTTP. On email failure, compensate by rolling back the DB insert.","tags":["invitations","email","transactions"]}
{"id":"p003","module":"002-auth","title":"queryForOrg for all org-scoped queries","body":"All org-scoped DB queries must use queryForOrg(orgId, userId, sql, params). Never use raw query() for org-scoped data. This enforces multi-tenancy at the DB layer.","tags":["multi-tenancy","db","rls"]}
{"id":"p004","module":"002-auth","title":"Login form uses signIn not Server Action","body":"The login form calls NextAuth signIn('credentials', {...}) directly — NOT a custom Server Action. Only signUp and acceptInvite use Server Actions. This is intentional — NextAuth manages session creation.","tags":["auth","forms","next-auth"]}
{"id":"p005","module":"003-layout-shell","title":"Shell architecture: Server wrappers + Client children","body":"Sidebar and Topbar are Server Components that receive user props from the async layout and render Client Component children (SidebarNav, UserAvatarMenu, etc.). Server Components cannot be imported by Client Components — data flows one-way from Server to Client via props.","tags":["shell","server-components","client-components"]}
{"id":"p006","module":"003-layout-shell","title":"Async Server Component unit test pattern","body":"DashboardLayout is async. In Vitest/JSDOM: call await DashboardLayout({children}) to get the JSX, then render(jsx). Mock getServerSession (next-auth/next), redirect (next/navigation), and child Server Components (Sidebar, Topbar) as simple stubs.","tags":["testing","server-components","layout"]}
{"id":"p007","module":"003-layout-shell","title":"orgName in NextAuth JWT","body":"orgName is NOT in NextAuth defaults. The authorize callback JOINs the orgs table (JOIN orgs o ON o.id = u.org_id) to fetch org_name. JWT callback writes token.orgName. Session callback reads token.orgName → session.user.orgName. Fallback gracefully to empty string if absent.","tags":["auth","jwt","session"]}
{"id":"p008","module":"004-shared-components","title":"TanStack Table v8: getCoreRowModel() only for server-driven tables","body":"With manualPagination: true and manualSorting: true, only getCoreRowModel() is needed. Do NOT add getSortedRowModel() or getPaginationRowModel() — the server handles all data logic. Adding client-side row models with manual flags causes double-processing.","tags":["tanstack-table","datatable","pagination","sorting"]}
{"id":"p009","module":"004-shared-components","title":"Shared form wrapper: consumer owns useForm, component accepts UseFormReturn<T>","body":"CrudForm (and any future shared form wrapper) accepts form: UseFormReturn<T> as a prop — it does NOT call useForm() internally. The consuming page owns the Zod schema, defaultValues, and useForm() call. This keeps shared wrappers schema-agnostic and reusable across all modules.","tags":["forms","react-hook-form","crudform","shared-components"]}
{"id":"p010","module":"004-shared-components","title":"Test ALL values of enum-like props, not just extremes","body":"When testing components with enum-like props (PriorityDot priority, StatusBadge status, ActivityTimeline type), write assertions for every valid value. Testing only Low and Urgent missed Medium (amber) and High (orange) — caught by adversarial review. One test per enum value is the floor.","tags":["testing","coverage","enums","adversarial-review"]}
{"id":"p011","module":"004-shared-components","title":"ACTIVITY_TYPE_CONFIG.bg encodes both background and text color","body":"The bg field in ACTIVITY_TYPE_CONFIG holds a compound Tailwind string e.g. 'bg-blue-100 text-blue-600'. Apply it to the icon container div via cn(config.bg) — it sets both background and icon color in one shot. Do not split into separate bg/text fields.","tags":["activity-timeline","tailwind","activity-type-config"]}
