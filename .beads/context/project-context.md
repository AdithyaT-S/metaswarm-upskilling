# Project Context (Maintained by Orchestrator)

## Tooling
- Package manager: pnpm
- Test runner: vitest (vitest.config.ts — TBD)
- Linter: eslint (next lint)
- Build: next build
- Platform: Windows 11 / PowerShell

## Pre-existing Files (DO NOT MODIFY)
- `src/lib/db/index.ts` — provider router: query(), transaction(), queryForOrg(orgId, userId, sql, params)
- `src/lib/db/types.ts` — DBProvider, DBProviderImpl, OrgContext
- `db/migrations/` — all migrations 000–004 pre-written
- `scripts/migrate.ts` + `scripts/seed.ts` — pre-written

## Completed Work Units
| WU | Title | Key Files | Services Created |
|----|-------|-----------|-----------------|
| (none yet) | | | |

## Established Patterns
(populated as WUs complete)

## Active Services
(populated as WUs complete)
