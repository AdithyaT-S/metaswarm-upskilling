import { test } from '@playwright/test'

test.describe('Logout flow from layout shell', () => {
  test('clicking avatar menu reveals Log out option on /dashboard', async () => {})
  test('clicking Log out redirects to /login', async () => {})
  test('navigating to /dashboard after logout redirects back to /login', async () => {})
  test('session is cleared after logout', async () => {})
})
