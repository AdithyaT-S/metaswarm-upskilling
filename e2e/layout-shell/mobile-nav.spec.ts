import { test } from '@playwright/test'

test.describe('Mobile navigation at 375px viewport', () => {
  test('hamburger button is visible at mobile viewport', async () => {})
  test('clicking hamburger button opens the navigation drawer', async () => {})
  test('clicking a nav item in the drawer navigates to the correct route', async () => {})
  test('clicking a nav item in the drawer closes the drawer', async () => {})
})
