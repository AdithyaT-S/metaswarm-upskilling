import { test } from '@playwright/test'

test.describe('Logout flow', () => {
  test('signs out user and redirects to /login', async () => {})
  test('clears session cookie after logout', async () => {})
})
